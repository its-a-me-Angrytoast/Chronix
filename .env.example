# ==============================================
# Chronix Environment Configuration Example
# ==============================================

# --- Core Bot Settings ---
# The Discord Bot Token (Required)
# Get this from the Discord Developer Portal -> Bot -> Token
TOKEN=

# The User ID of the bot owner (Required for owner-only commands)
OWNER_ID=

# The Server ID where development commands are registered immediately (Optional but recommended for dev)
DEV_GUILD_ID=

# Enable development mode (more logging, immediate command sync)
DEV_MODE=true

# Force owner override to allow owner commands in any server
FORCE_OWNER_OVERRIDE=false

# --- Dashboard Configuration ---
# Discord Application Credentials (Required for Dashboard Login)
# Get these from Discord Developer Portal -> OAuth2
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# The URL Discord redirects to after login
# For local dev: http://localhost:9091/api/auth/callback
# For production: https://your-domain.com/api/auth/callback
DISCORD_REDIRECT_URI=http://localhost:9091/api/auth/callback

# A random secret string for signing session cookies (Required)
SECRET_KEY=change_this_to_a_long_random_string

# The Invite Link used in the Dashboard "Add to Discord" button
# Ensure the 'redirect_uri' match DISCORD_REDIRECT_URI if using 'response_type=code' flows
VITE_DISCORD_INVITE_URL=https://discord.com/oauth2/authorize?client_id=YOUR_CLIENT_ID&permissions=8&scope=bot%20applications.commands

# Dashboard Host/Port Settings
CHRONIX_DASHBOARD_HOST=127.0.0.1
CHRONIX_DASHBOARD_PORT=8081
CHRONIX_DASHBOARD_RPC_HOST=127.0.0.1
CHRONIX_DASHBOARD_RPC_PORT=9091
CHRONIX_DASHBOARD_POLL_INTERVAL=6
CHRONIX_DASHBOARD_STANDALONE=true
CHRONIX_DASHBOARD_MAINTENANCE=false
CHRONIX_DASHBOARD_OPEN_BROWSER=false

# API Key for securing internal dashboard RPC (Optional, defaults to none for local)
CHRONIX_DASHBOARD_API_KEY=

# --- Database Settings ---
# PostgreSQL Connection URL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost/chronix
# Alias often used by libraries
DATABASE_DSN=postgresql://user:password@localhost/chronix

# Connection Pool Settings
DB_POOL_MIN=1
DB_POOL_MAX=10

# --- AI Integration (Optional) ---
# Google Gemini
GEMINI_API_KEY=
GEMINI_API_URL=

# OpenAI
OPENAI_API_KEY=
OPENAI_API_URL=https://api.openai.com/v1/chat/completions
OPENAI_MODEL=gpt-4o

# --- Music Configuration (Lavalink) ---
# Required if Music cog is enabled
LAVALINK_HOST=127.0.0.1
LAVALINK_PORT=2333
LAVALINK_AUTH=youshallnotpass

# Spotify Integration (for track metadata)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# YouTube Integration (for search)
YOUTUBE_API_KEY=

# Music Feature Toggles
ENABLE_MUSIC_REQUEST_CREDITS=false
MUSIC_REQUEST_CREDIT_COST=10
MUSIC_STATS_ENABLED=true

# --- XP / Leveling System ---
XP_MESSAGE_COOLDOWN=60
XP_MIN_PER_MESSAGE=5
XP_MAX_PER_MESSAGE=12
XP_ROLE_MULTIPLE=5

# --- Logging & Maintenance ---
DEV_LOG_CHANNEL_ID=
LOG_PRUNE_ENABLED=true
LOG_RETENTION_DAYS=30
LOG_PRUNE_INTERVAL_HOURS=24

# --- Testing & Internal ---
# Skip heavy DB syncs during startup
FAST_SYNC=false
# Dashboard internal data directory
CHRONIX_DATA_DIR=data