# Chronix Dashboard

This is the frontend for the Chronix Discord bot dashboard.

## Development

1.  Navigate to the `dashboard` directory: `cd dashboard`
2.  Install dependencies: `npm install`
3.  Start the development server: `npm run dev`
